
<!DOCTYPE html>
<html lang="bn">
<head>
  <meta charset="UTF-8" />
  <title>অ্যাডমিন উইথড্র রিকোয়েস্ট</title>
  <style>
    body { background: black; color: white; text-align: center; font-family: sans-serif; }
    table { margin: auto; border-collapse: collapse; margin-top: 20px; }
    td, th { border: 1px solid white; padding: 8px 15px; }
    button { margin: 2px; padding: 4px 10px; }
  </style>
</head>
<body>
  <h2>💼 অ্যাডমিন উইথড্র রিকোয়েস্ট প্যানেল</h2>
  <table>
    <thead>
      <tr><th>ইমেইল</th><th>টাকা</th><th>স্ট্যাটাস</th><th>অ্যাকশন</th></tr>
    </thead>
    <tbody id="requestTable">
      <tr><td colspan="4">লোড হচ্ছে...</td></tr>
    </tbody>
  </table>

  <script type="module">
    import { db } from "./firebase.js";
    import {
      collection, getDocs, doc, updateDoc
    } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js";

    const table = document.getElementById("requestTable");

    async function loadRequests() {
      const snapshot = await getDocs(collection(db, "windowrequest"));
      table.innerHTML = "";
      snapshot.forEach(docSnap => {
        const data = docSnap.data();
        const row = document.createElement("tr");
        row.innerHTML = `
          <td>${data.email}</td>
          <td>${data.amount} টাকা</td>
          <td>${data.status}</td>
          <td>
            <button onclick="approveRequest('${docSnap.id}')">Approve</button>
            <button onclick="rejectRequest('${docSnap.id}')">Reject</button>
          </td>
        `;
        table.appendChild(row);
      });
    }

    window.approveRequest = async (id) => {
      await updateDoc(doc(db, "windowrequest", id), { status: "Approved" });
      loadRequests();
    };

    window.rejectRequest = async (id) => {
      await updateDoc(doc(db, "windowrequest", id), { status: "Rejected" });
      loadRequests();
    };

    window.onload = loadRequests;
  </script>
</body>
</html>
